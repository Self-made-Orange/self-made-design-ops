# 의존성
node_modules/

# 환경 변수 — FIGMA_OAUTH_TOKEN 등이 여기 들어갑니다. 절대 커밋하지 마세요.
.env
.env.*
!.env.example

# Claude 로컬 설정 (MCP 서버 설정에 토큰이 포함될 수 있음)
.claude/settings.local.json

# 수집 작업 중 생기는 임시 파일
*.tgz
/tmp/
scratch/

# OS / 에디터
.DS_Store
Thumbs.db
.idea/
.vscode/

# 발행 초안 — 로컬 작업물입니다. 저장소에는 올리지 않습니다.
# 이미 올라갔던 것을 되돌린 것이라, ea6c41e 이후의 과거 커밋에는 그대로 남아 있습니다.
content/publications/
